import { httpRouter } from "convex/server";

/**
 * Convex HTTP routes
 * Better Auth handles auth via Next.js; add custom endpoints here if needed.
 */
const http = httpRouter();

export default http;
